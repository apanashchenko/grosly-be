# 🍽️ Grosly - Генератор списків продуктів з рецептів

Додаток для автоматичної генерації списків продуктів з рецептів за допомогою штучного інтелекту (OpenAI GPT).

## 🎯 Функціонал

- ✅ Парсинг рецептів і витягування інгредієнтів
- ✅ Структурування даних (назва, кількість, одиниця виміру)
- ✅ Підтримка української мови
- 🔄 Планується: парсинг з URL, збереження в БД, уподобання користувачів

## 🚀 Швидкий старт

### 1. Встановлення залежностей

```bash
pnpm install
```

### 2. Налаштування

Створіть файл `.env` на основі `.env.example`:

```bash
cp .env.example .env
```

Отримайте OpenAI API ключ:
1. Перейдіть на https://platform.openai.com/api-keys
2. Створіть новий API ключ
3. Додайте його в `.env`:

```env
OPENAI_API_KEY=sk-your-api-key-here
PORT=3000
```

### 3. Запуск

```bash
# Development mode
pnpm start:dev

# Production mode
pnpm build
pnpm start:prod
```

Сервер буде доступний на `http://localhost:3000`

## 📡 API Endpoints

### POST `/recipes/parse`

Парсинг рецепту та витягування інгредієнтів.

**Request:**
```json
{
  "recipeText": "Борщ\n\nІнгредієнти:\n- буряк 2 шт\n- картопля 3 шт\n- морква 1 шт\n- цибуля 1 шт\n- капуста 300 г\n- м'ясо 500 г\n- томатна паста 2 ст.л.\n- сіль, перець за смаком"
}
```

**Response:**
```json
{
  "ingredients": [
    {
      "name": "буряк",
      "quantity": "2",
      "unit": "штук"
    },
    {
      "name": "картопля",
      "quantity": "3",
      "unit": "штук"
    },
    {
      "name": "морква",
      "quantity": "1",
      "unit": "штук"
    },
    {
      "name": "цибуля",
      "quantity": "1",
      "unit": "штук"
    },
    {
      "name": "капуста",
      "quantity": "300",
      "unit": "грам"
    },
    {
      "name": "м'ясо",
      "quantity": "500",
      "unit": "грам"
    },
    {
      "name": "томатна паста",
      "quantity": "2",
      "unit": "ст.л."
    },
    {
      "name": "сіль",
      "quantity": "за смаком",
      "unit": "за смаком"
    },
    {
      "name": "перець",
      "quantity": "за смаком",
      "unit": "за смаком"
    }
  ]
}
```

## 🧪 Тестування з curl

```bash
curl -X POST http://localhost:3000/recipes/parse \
  -H "Content-Type: application/json" \
  -d '{
    "recipeText": "Борщ\n\nІнгредієнти:\n- буряк 2 шт\n- картопля 3 шт\n- морква 1 шт\n- цибуля 1 шт\n- капуста 300 г\n- м'\''ясо 500 г\n- томатна паста 2 ст.л.\n- сіль, перець за смаком"
  }'
```

## 🏗️ Архітектура

```
src/
├── ai/                     # AI модуль (OpenAI)
│   ├── ai.module.ts
│   └── ai.service.ts
├── recipes/                # Модуль рецептів
│   ├── dto/
│   │   ├── ingredient.dto.ts
│   │   └── parse-recipe.dto.ts
│   ├── recipes.controller.ts
│   ├── recipes.service.ts
│   └── recipes.module.ts
├── app.module.ts           # Головний модуль
└── main.ts                 # Entry point
```

## 🛠️ Технології

- **Backend:** NestJS
- **AI:** OpenAI GPT-3.5 Turbo
- **Валідація:** class-validator, class-transformer
- **База даних:** PostgreSQL (планується)
- **Парсинг:** Cheerio, Playwright (для майбутнього функціоналу)

## 💰 Вартість використання

OpenAI GPT-3.5 Turbo:
- ~$0.0005 (input) + $0.0015 (output) за 1K tokens
- Середній рецепт: ~$0.002-0.003 за запит

## 📝 Roadmap

- [x] Парсинг текстових рецептів
- [ ] Парсинг рецептів з URL
- [ ] Збереження рецептів в БД
- [ ] Профілі користувачів
- [ ] Уподобання та дієтичні обмеження
- [ ] Планування меню на тиждень
- [ ] Frontend (React/Vue)
- [ ] Мобільний додаток

## 📄 Ліцензія

UNLICENSED
